// const scrollStep = (offsetTop, intervalId) => {
//   const scrollStepInPx = '50';
//   if (window.pageYOffset === 0) {
//     clearInterval(intervalId);
//   }
//   window.scroll(0, offsetTop - scrollStepInPx);
// };

// export { scrollStep };
